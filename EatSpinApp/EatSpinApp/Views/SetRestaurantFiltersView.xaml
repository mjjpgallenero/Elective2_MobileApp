<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:EatSpinApp.ViewModels;assembly=EatSpinApp"
             x:Class="EatSpinApp.Views.SetRestaurantFiltersView">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Apply Filters"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding RestaurantTags, Mode=TwoWay}" 
                      SelectedItem="{Binding SelectedRestaurantTag, Mode=TwoWay}" 
                      HasUnevenRows="True" 
            >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Padding="10"/>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Text="Delete Entry" Command="{Binding DeleteRestaurantCommand}"/>
            <Button Text="Add Entry" Command="{Binding OpenAddNewRestaurantView}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>